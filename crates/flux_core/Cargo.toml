[package]
name = "flux_core"
version = "0.0.0"
edition.workspace = true
license.workspace = true
authors.workspace = true

[lib]
path = "src/lib.rs"

[dependencies]
serde = { version = "1", features = ["derive"] }
semver = "1"
thiserror = "2"

[dev-dependencies]
serde_json = "1"
